import { Flex } from "@radix-ui/themes"

export default function Creation ({ children, }: Readonly<{children: React.ReactNode;}>) {
    return (
        <Flex height="100vh" width="100vw" direction="column">
            <Flex>

            </Flex>
            <Flex>

            </Flex>
        </Flex>
    );
}